import React from 'react';
import { Text, ScrollView, Button } from 'react-native';
import { useTheme } from 'styled-components/native';
import { useThemeStore } from '../store/themeStore';
import Card from '../components/Card';
import Tag from '../components/Tag';

export default function Dashboard({ navigation }) {
  const theme = useTheme();
  const { currentTheme } = useThemeStore();

  return (
    <ScrollView style={{ backgroundColor: theme.background, flex: 1, padding: 20 }}>
      <Text style={{ fontSize: 24, color: theme.text, marginBottom: 16 }}>
        Good {currentTheme === 'growth' ? 'Calm' : 'Power'} Day 🌤️
      </Text>

      <Card>
        <Text style={{ fontSize: 18, marginBottom: 4, color: theme.text }}>Today</Text>
        <Tag label="2 Habits" />
        <Tag label="1 in Progress" />
      </Card>

      <Card>
        <Text style={{ fontSize: 18, marginBottom: 4, color: theme.text }}>Projects</Text>
        <Tag label="Learning Design" />
        <Tag label="Ongoing" />
        <Button title="View" onPress={() => navigation.navigate('HabitEditor')} />
      </Card>

      <Button title="Settings" onPress={() => navigation.navigate('Settings')} />
    </ScrollView>
  );
}
